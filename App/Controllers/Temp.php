<?php 
namespace App\Controllers;
use PDO;
use \App\Token;
use \App\Mail;
use \Core\View;

public function __construct(var temp, var t)
    {
        foreach ($data as $key => $value) {
            $this->$key = $value;
        };
    }
    ?>